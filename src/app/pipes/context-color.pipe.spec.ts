import { ContextColorPipe } from './context-color.pipe';

describe('ContextColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ContextColorPipe();
    expect(pipe).toBeTruthy();
  });
});
