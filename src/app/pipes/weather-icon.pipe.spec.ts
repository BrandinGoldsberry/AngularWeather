import { WeatherIconPipe } from './weather-icon.pipe';

describe('WeatherIconPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WeatherIconPipe();
    expect(pipe).toBeTruthy();
  });
});
