import { NumberRoundPipe } from './number-round.pipe';

describe('NumberRoundPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberRoundPipe();
    expect(pipe).toBeTruthy();
  });
});
